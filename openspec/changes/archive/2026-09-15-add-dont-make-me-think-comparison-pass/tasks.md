## 1. Rule content — usability

- [x] 1.1 Add new rule `VH04` to `src/skills/usability/references/visual-hierarchy.md` — non-floating headings (a heading sits closer to the text it introduces than to the section above it; `margin-top > margin-bottom`), per `specs/ux-crux/usability/spec.md`'s "Headings sit closer to the content they introduce" requirement. Update the file's closing "How the usability skill should apply these" prose to mention `VH04` alongside `VH01`-`VH03`. Verify by re-reading the file and confirming the new row and prose reference are present and consistent with the delta spec.
- [x] 1.2 Add new rule `N24R` to `src/skills/usability/references/core.md`'s Navigation table — multi-cue "You Are Here" active-navigation-state indication, distinct from `VH01`'s general near-uniformity concern, per the delta spec's "Active navigation state uses multiple simultaneous visual cues" requirement. Update the Navigation section's lens-selection prose paragraph to reference `N24R` alongside the existing `N19R`-`N23R` mentions. Verify by re-reading the file.
- [x] 1.3 Add new rule `AF06` to `src/skills/usability/references/affordances.md` — flat-design affordance risk, per the delta spec's "Clickable controls remain visually distinguishable under flat/minimalist design" requirement. Update the file's closing "How the usability skill should apply these" prose to mention `AF06` alongside `AF01`-`AF05`. Verify by re-reading the file.
- [x] 1.4 Add new rule `D09` to `src/skills/usability/references/core.md`'s Search/discovery table — uncluttered global search box standard (no forced pre-search scoping, no unclear placeholder-based labeling in place of a plain "Search" affordance), per the delta spec's "Global search input avoids forced scoping and unclear labeling" requirement. Update the Search/discovery lens-selection prose to reference `D09` alongside `D07`-`D08`. Verify by re-reading the file.

## 2. Rule content — accessibility

- [x] 2.1 Add new rule `X17` to `src/skills/accessibility/references/core.md` — hover-dependent affordances (tooltips, submenus, clickability cues shown only on mouse-hover) must have a tap/focus-triggered or always-visible equivalent, per the delta spec's "Hover-dependent affordances have a non-hover equivalent" requirement. Verify by re-reading the file.
- [x] 2.2 Add new rule `X18` to `src/skills/accessibility/references/core.md` — preserved visited-vs-unvisited link color distinction, per the delta spec's "Visited and unvisited links remain visually distinct" requirement. Verify by re-reading the file.
- [x] 2.3 Add new rule `X19` to `src/skills/accessibility/references/core.md` — front-loaded link/heading keyword for screen-reader ear-scanning, explicitly distinct from `product`'s `C07`/`C08` (sighted long-form scanning), per the delta spec's "Link and heading text front-loads its distinguishing keyword" requirement. Verify by re-reading the file.
- [x] 2.4 Add new rule `X20` to `src/skills/accessibility/references/core.md` — a "skip to main content" link precedes persistent header navigation as the first focusable element, per the delta spec's corresponding requirement. Verify by re-reading the file.
- [x] 2.5 Add new rule `X21` to `src/skills/accessibility/references/core.md` — logical, unskipped heading hierarchy reflecting content structure rather than default visual size, per the delta spec's corresponding requirement. Verify by re-reading the file.
- [x] 2.6 Add new rule `X22` to `src/skills/accessibility/references/core.md` — images distinguish informative alt text from empty `alt=""` for decorative images, per the delta spec's corresponding requirement. Verify by re-reading the file.
- [x] 2.7 Update `src/skills/accessibility/references/core.md`'s "How the accessibility skill should apply these" prose and `src/skills/accessibility/SKILL.md`'s numbered procedure to reference the six new rules (`X17`-`X22`) at the appropriate step, and add each new rule's typical evidence-assessability note (most are code/markup-dependent and `NOT ASSESSABLE` from a static screenshot alone, consistent with existing `X01`/`X08` framing). Verify by re-reading both files.

## 3. Rule content — trust

- [x] 3.1 Add new rule `GW01` to `src/skills/trust/references/core.md` — the Reservoir of Goodwill model plus its depletors (hidden information, unnecessary data requests, hollow sincerity copy, task-blocking promotion, amateurish execution), introducing the new `GW` rule-ID prefix per `design.md`'s Decisions section, per the delta spec's "Interface friction is evaluated against a user's situational goodwill reservoir" requirement. Explicitly note the distinction from the existing `CT01` (cost-disclosure timing). Verify by re-reading the file.
- [x] 3.2 Add new rule `GW02` to `src/skills/trust/references/core.md` — rigid input-formatting rejection (a validly-formatted card/phone number rejected instead of normalized), explicitly distinct from `usability`'s `F04`, per the delta spec's corresponding requirement. Verify by re-reading the file.
- [x] 3.3 Add new rule `GW03` to `src/skills/trust/references/core.md` — goodwill refills (candid FAQs vs. marketing-dressed FAQ, graceful/actionable error recovery, step-saving shortcuts embedded in routine communication), per the delta spec's "Support and error-recovery content actively restores user goodwill" requirement. Verify by re-reading the file.
- [x] 3.4 Update `src/skills/trust/references/core.md`'s applying-these prose (or add one if none exists) to reference `GW01`-`GW03` and confirm the new `GW` prefix does not collide with any existing prefix in the file. Verify by re-reading the file and confirming no duplicate rule ID exists across `src/skills/trust/references/*.md`.

## 4. Rule content — product

- [x] 4.1 Add new rule `C31` to `src/skills/product/references/core.md` — tagline excellence criteria (roughly 6-8 words, concrete differentiating value proposition, not a vague corporate motto), per the delta spec's "A tagline next to site/product identity states a concrete value proposition" requirement, including the household-brand exception scenario. Update the file's applying-these prose to reference `C31`. Verify by re-reading the file.

## 5. Rule content — review

- [x] 5.1 Add new rule `V09` to `src/skills/review/references/review-model.md`'s validation-methodology table — the Trunk Test as a standing checklist for interior/deep pages (Site ID, Page Name, Sections, Local Navigation, "You Are Here", Search), modeled on `V07`'s structural pattern, per the delta spec's "Interior pages pass a standing Trunk Test for orientation" requirement, including the homepage-only and single-lens-narrowed exception scenarios. Update the closing summary paragraph after the table (the one explaining why `V01`-`V08` matter) to add a sentence on `V09`'s purpose, following the pattern used for `V07`/`V08`. Update the "How the review skill should apply this file" closing prose and the lens-selection procedure's step 1 to reference `V09`. Verify by re-reading the file.

## 6. Shared severity-model clarification

- [x] 6.1 Add a prose clarification (no new ID; `severity-model.md` has no ID scheme) to `src/shared/severity-model.md`'s "Rule for skills" section — a momentary error a user notices and self-corrects within a moment or two, with no lasting confusion or lost work (Krug's "kayak problem"), is not scored at the same severity as a genuine roadblock; this refines how the existing four severity levels are applied, it does not add a new level or a parallel vocabulary. Verify by re-reading the file and confirming the existing Blocker/Major/Moderate/Minor definitions are unchanged.

## 7. Documentation

- [x] 7.1 Add Steve Krug, *Don't Make Me Think, Revisited: A Common Sense Approach to Web Usability* (3rd ed.), to `README.md`'s "Evidence base" section, matching the existing citation format used for other books in that list.
- [x] 7.2 Recount every rule-table row across `src/skills/**/references/*.md` (rows matching the `| <ID> | ... |` pattern; exclude folk-rule-guard and other non-rule tables) and update the total rule count stated in `README.md`'s opening paragraph. Verify by running the recount (grep count or equivalent) and confirming the stated number matches exactly (expected: +15 from this change — `VH04`, `N24R`, `AF06`, `D09`, `X17`-`X22`, `GW01`-`GW03`, `C31`, `V09`).
- [x] 7.3 Add a new `CHANGELOG.md` entry for this comparison pass, in the same structure and prose style as the prior 18 comparison-pass entries: name the source book, list every new rule ID added (grouped by domain), explicitly confirm what was already covered and left untouched (`A13R`, `N21R`, `PA09`, `PC08`/`P03`/`P04`, `psychology`'s ethics/behavioral-economics files, `N16R`/`N23R`), and explain why the remainder of the book (DIY usability-testing cadence, focus-group-vs-test distinction, participant recruiting, facilitator technique, debrief logistics, executive-advocacy tactics, "religious debates" team dynamics, the myth of the average user as a team assumption) was scoped out as team-process/testing-methodology content rather than static-evidence-checkable UI content, consistent with every prior comparison pass.

## 8. Release gate

- [x] 8.1 Bump the version in `package.json` per this project's SemVer practice for a rule-adding change (15 new rules across 4 domains plus one shared-model clarification warrants at least a minor bump), then run `npm run build && npm run sync-version && npm run validate` and confirm it completes successfully with no errors. This regenerates `skills/` and `plugin/` from `src/` and syncs the version into both plugin manifests — do not hand-edit the generated output.
