## 1. Product: feature/permission justification rule

- [x] 1.1 In `src/skills/product/references/core.md`, add rule `P08` to the "Purpose & task" table (next available ID after `P07`): a feature, permission request, or onboarding flow must be evaluable against a concrete user value, why it's the best available way to serve that value, and explicit operating boundaries — flag vague, open-ended value propositions used to justify unconstrained access. Follow the existing table's column format (ID / Rule / Evidence / Default severity / Sources); cite the Fogg Behavior Model and Apple/Android's own permission-justification guidance, not the book. Verify the new row matches the existing table's format and that no `P0x`/`IA0x`/`C0x` ID is reused or renumbered.
- [x] 1.2 Update `src/skills/product/references/core.md`'s "How the product skill should apply these" section to add a step for `P08`, explicitly distinguishing it from `P06` (identity/data gating vs. any feature's value/justification) and including the `NOT ASSESSABLE` guard when alternative-approach context is missing. Verify the addition reflects all three scenarios in `specs/ux-crux/product/spec.md`.
- [x] 1.3 Update `src/skills/product/SKILL.md`'s Procedure and References sections to mention `P08` and its location in `references/core.md`. Verify the skill file still accurately describes everything `core.md` now covers.

## 2. Trust: notification granularity and badge urgency-color rules

- [x] 2.1 In `src/skills/trust/references/mobile.md`, add rule `O11` to the "Permission and notification timing" table (next available ID after `O10`): notification controls should support time-based quiet hours and sender/contact-level allowlisting, not just category toggles. Cite Apple Focus and Android Do Not Disturb + priority senders. Verify the new row matches the existing table's format and `O04`-`O10` are unchanged.
- [x] 2.2 In the same file, add rule `I06` to the "Interruptions" table (next available ID after `I05`): a badge/unread indicator's color or visual intensity must not signal higher urgency than the content's actual criticality. Cite Apple HIG's badge-semantics guidance ("supplement notifications, not... critical information") together with published color-arousal/urgency-perception research; a well-known industry badge-color anecdote may appear only as a "useful example," never as the cited evidence. Verify the new row matches the existing table's format and `I01`-`I05` are unchanged.
- [x] 2.3 Update `src/skills/trust/references/mobile.md`'s "How the trust skill should apply these" section to cover `O11` (alongside the existing `O09` framing, noting sender-level allowlisting only applies where the product has a notion of distinct senders/contacts) and `I06` (anchored on a mismatch between visual urgency and actual criticality, not on any specific color being inherently disqualifying). Verify the update reflects all six scenarios across both new requirements in `specs/ux-crux/trust/spec.md`.
- [x] 2.4 Update `src/skills/trust/SKILL.md`'s Procedure and References sections so any "O04-O10"/"I01-I05" reference becomes "O04-O11"/"I01-I06". Verify by re-reading the skill file for a reader who has not seen the new rules.

## 3. Psychology: conversation-channel bandwidth rule and mobile-severity note

- [x] 3.1 In `src/skills/psychology/references/social.md`, add rule `PT07` to the existing table (next available ID after `PT06`): for relationship-maintenance use cases, flag designs that position low-bandwidth async interaction as a full substitute for richer conversation, and recommend routing toward voice/video/in-person. Cite Sherry Turkle (MIT), *Reclaiming Conversation*. Verify the new row matches the existing table's format and `PT01`-`PT06` are unchanged.
- [x] 3.2 Update `src/skills/psychology/references/social.md`'s "How the psychology skill should apply these" section to cover `PT07`, including the logistics-only exception (purely functional info transfer stays out of scope). Verify the update reflects all three scenarios for the conversation-channel-bandwidth requirement in `specs/ux-crux/psychology/spec.md`.
- [x] 3.3 In `src/skills/psychology/references/habits.md`'s "How the psychology skill should apply these" section, add a note that an engagement-loop finding under `PH02`-`PH04` gets a default severity at the higher end of the applicable band (per `../../shared/severity-model.md`) when observed on mobile vs. an equivalent desktop/web finding, because the device is carried on-body and continuously reachable. Do not add a new `PH0x` ID or alter the `PH02`-`PH04` table rows themselves. Verify the update reflects both scenarios for the mobile-severity-calibration requirement in `specs/ux-crux/psychology/spec.md`, and that `severity-model.md` itself is left unchanged.
- [x] 3.4 Update `src/skills/psychology/SKILL.md` if it lists or summarizes `social.md`'s rule range, so any "PT01-PT06" reference becomes "PT01-PT07". Verify by re-reading the skill file for a reader who has not seen the new rule.

## 4. Build, version, and validation gate

- [x] 4.1 Run `npm run build` and verify it regenerates `skills/ux-crux-product`, `skills/ux-crux-trust`, `skills/ux-crux-psychology`, `plugin/skills/product`, `plugin/skills/trust`, and `plugin/skills/psychology` with no manual edits to generated files.
- [x] 4.2 Bump the version in `package.json`, then run `npm run sync-version` and verify the bump is reflected in both plugin manifests.
- [x] 4.3 Run `npm run validate` and verify it passes (distributed-content version-bump check and shared/ drift check both clean).
