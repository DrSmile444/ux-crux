## 1. Rule content

- [x] 1.1 Add new rule `N22R` to `src/skills/usability/references/core.md`'s Navigation table — goal-altitude consistency across sibling navigation/menu items (Alistair Cockburn's Cloud/Sea-level/Fish-level taxonomy, cited via Jeff Patton, *User Story Mapping*), distinct from `N20R`'s mutual-exclusivity/overlap scope. Update the Navigation section's lens-selection prose paragraph in the same file to reference `N22R` alongside the existing `N19R`-`N21R` mentions. Verify by re-reading the file and confirming the new row and prose reference are present and consistent with the delta spec's requirement text in `specs/ux-crux/usability/spec.md`.
- [x] 1.2 Add new rule `V08` to `src/skills/review/references/review-model.md`'s validation methodology table — an "MVP"/beta/early-access label does not lower a finding's applicable severity (Jeff Patton, *User Story Mapping*, Minimum Viable Solution vs. "crappiest product" distinction). Update the closing summary paragraph after the table (the one explaining why V01-V07 matter) to include a one-sentence mention of V08's purpose, following the same pattern used for V07. Verify by re-reading the file and confirming the new row and summary sentence are present and consistent with `specs/ux-crux/review/spec.md`.
- [x] 1.3 Add a citation-only enrichment (no new rule ID, no behavior change) to `V04`'s Sources column in `src/skills/review/references/review-model.md`, naming Jeff Patton's "What-About" edge-case-probing technique (via David Hussman) as a discovery-side counterpart to the same happy-path-blindness concern V04 already covers. Verify by re-reading the row and confirming no other cell in the row changed.

## 2. Documentation

- [x] 2.1 Add Jeff Patton (with Peter Economy), *User Story Mapping: Discover the Whole Story, Build the Right Product* (O'Reilly Media), to `README.md`'s "Evidence base" section, matching the existing citation format used for other books in that list. Verify by re-reading the section and confirming the new entry is present and correctly formatted.
- [x] 2.2 Recount every rule-table row across `src/skills/**/references/*.md` (rows matching the `| <ID> | ... |` pattern; exclude folk-rule-guard and other non-rule tables) and update the total rule count stated in `README.md`'s opening paragraph. Verify by running the recount command/script (or manual grep count) and confirming the stated number matches exactly (expected: +2 from this change, for `N22R` and `V08`).
- [x] 2.3 Add a new `CHANGELOG.md` entry for this comparison pass, in the same structure and prose style as the prior 17 comparison-pass entries (0.1.6-0.1.18): name the source book, state what was added (`N22R`, `V08`, the `V04` citation enrichment), explicitly confirm Item 20 (organizational-jargon-leak-into-UI) was already covered by `product`'s existing `IA04` and left untouched, and explain why the remainder of the book's content (team-process, discovery, and delivery methodology — story mapping workshops, Three Amigos, Design Studio, Fishbowl, walking skeleton, cupcake strategy, story splitting/bundling, Opportunity Canvas, risk stories, the 3 Cs, template zombie, library card catalog, information radiators, the Discovery Triad, waiter-vs-doctor, Now Map/Later Map, and the rest) was scoped out as describing how a team runs its own process rather than what a static-evidence review checks, consistent with every prior comparison pass. Verify by re-reading the entry against the existing entries' structure (Why/what-was-added/what-was-confirmed/what-was-scoped-out).

## 3. Release gate

- [x] 3.1 Bump the version in `package.json` per this project's SemVer practice for a rule-adding change, then run `npm run build && npm run sync-version && npm run validate` and confirm it completes successfully with no errors. This regenerates `skills/` and `plugin/` from `src/` and syncs the version into both plugin manifests — do not hand-edit the generated output.
