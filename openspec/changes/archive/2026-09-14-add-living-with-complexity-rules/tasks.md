## 1. Usability: design-side signage-reliance signal in `affordances.md`

- [x] 1.1 Add `AF05` (a persistent instructional label, warning sticker, or explanatory tooltip added by the design itself to compensate for a control's own confusing operation is evidence the design failed, not a mitigation to credit; legally mandated warnings excluded) to `src/skills/usability/references/affordances.md`, matching the "A permanent hint bubble compensates for an unclear icon-only control", "A static instructional sign is posted because users repeatedly fail a step", and "A legally mandated compliance warning is present" scenarios in `specs/ux-crux/usability/spec.md`; explicitly name `AF02` in the rule text as the mirror-image, user-side counterpart.
- [x] 1.2 Update the file's intro line ("Hartson & Pyla's Four Kinds of Affordances... and one rule (AF02)...") to mention `AF05`, and update the closing "Apply AF01 as a diagnostic step..." paragraph to add guidance for when to apply `AF05` (whenever the evidence shows a persistent explanatory artifact the design itself added, as distinct from `AF02`'s user-added artifact).

## 2. Usability: wait-design, forward-step-visibility, and checklist additions in `core.md`

- [x] 2.1 Add `S13` (a wait/queue experience sets an appropriately conservative duration/position expectation, keeps the user occupied, treats all waiting users under one visibly fair order, and ends on a clear positive resolution) to the System status table, matching the "A checkout or processing wait shows no duration estimate and a blank screen", "A support-ticket or service queue allows undisclosed priority cuts", and "A wait experience sets a conservative estimate, offers relevant content, and ends clearly" scenarios in `specs/ux-crux/usability/spec.md`; explicitly distinguish it from `S03`/`S04` (loading-state visual treatment) and `S06` (state completeness) in the rule text.
- [x] 2.2 Add `N23R` (a multi-step wizard/flow shows the current step and the total or expected remaining steps, or an explicit not-yet-knowable signal) to the Navigation table, matching the "A multi-step signup flow shows no indication of remaining steps", "A flow's total step count is genuinely not knowable in advance", and "A wizard shows both current step and total step count" scenarios; explicitly distinguish it from `N16R` (breadcrumbs/sitemap — the past dimension) and `C03` (label consistency, not step-count exposure).
- [x] 2.3 Add `E09` (a long, sequential, high-consequence procedure provides an explicit, interactive step-by-step checklist with persistent checked/unchecked state and surfaces skipped steps) to the Errors & recovery table, matching the "A long compliance submission has no step-tracking and a required field is silently skipped", "A short, low-consequence flow has no checklist", and "A safety-critical setup flow provides a persistent, interactive checklist" scenarios; explicitly distinguish it from `CR01` (single-action emergency response) and `PC06` (general interruption/state resilience) in the rule text.
- [x] 2.4 Update `core.md`'s "How the usability skill should apply these" closing section to mention `S13` alongside `S11`/`S12`, `N23R` alongside the `N13R`-`N22R` set, and `E09` alongside the existing error-handling checks.

## 3. Usability: citation-only enrichments in `core.md` (no new rule IDs)

- [x] 3.1 Add a citation to Norman's "the natural world has no error messages" framing (via *Living with Complexity*) to `E01`'s Sources column, with a short clause naming this framing as further support for eliminating error messages via constraints/prevention rather than relying on them — no wording change to `E01`'s existing `SHALL`-equivalent behavior.
- [x] 3.2 Add a citation to Norman's security-usability inverse paradox and the 5,000-password encrypted-file anecdote to `F10`'s and `F11`'s Sources columns, with a short clause naming onerous security friction as the mechanism that produces insecure workarounds — no behavior change to either rule.

## 4. Psychology: multiplicity/scaling-burden addition and citation enrichments in `cognitive.md`

- [x] 4.1 Add `PC09` (the cumulative cognitive burden of many individually-simple, idiosyncratic interactions across a product is evaluated as a system-level load, not dismissed because any single instance is simple in isolation) to `src/skills/psychology/references/cognitive.md`, matching the "Many individually-reasonable confirmations compound into system-level burden", "A repeated action is simple, consistent, and centrally managed", and "A single simple interaction is evaluated on its own" scenarios in `specs/ux-crux/psychology/spec.md`; explicitly distinguish it from `PC01` (extraneous load within one task) in the rule text.
- [x] 4.2 Add a citation to Norman's multiplicity/scaling-problem framing and the door-knob/password-sticky-note examples (via *Living with Complexity*) to `PC01`'s Sources column as reinforcing context for why `PC09` is a distinct, system-level concern — no behavior change to `PC01`.
- [x] 4.3 Add a citation to Norman's perceived-simplicity-vs-operational-simplicity framing and the scientific-calculator-vs-sparse-menu example to `PC08`'s Sources column, sharpening its existing "frequently needed hidden options can increase cognitive and interaction cost" rule — no behavior change.
- [x] 4.4 Add a citation to Norman's "welcome back" contextual-resumption framing and the interrupted-multi-step-form example to `PC06`'s Sources column, sharpening its existing state-preservation-on-interruption rule — no behavior change.
- [x] 4.5 Update `cognitive.md`'s "How the psychology skill should apply these" closing paragraph to mention `PC09` alongside the existing cost-side-of-the-ledger framing.

## 5. Psychology: citation enrichment in `behavioral-economics.md` (no new rule ID)

- [x] 5.1 Add a citation to Norman's opt-in-vs-opt-out pension/401(k)-enrollment example (via *Living with Complexity*) to `PB01`'s and `PB02`'s Sources columns, as further named-source backing for the existing defaults rules — no behavior change to either rule.

## 6. Product: activity-centered control grouping in `information-architecture.md`

- [x] 6.1 Add `IA10` (controls for a cross-module user activity are grouped and exposed by that named activity, not by which internal module or device owns them) to `src/skills/product/references/information-architecture.md`, matching the "A compound task requires configuring several separate modules in sequence", "A single module inherently requires its own dedicated controls", and "Controls are already grouped by named activity" scenarios in `specs/ux-crux/product/spec.md`; explicitly distinguish it from `IA01` (content/navigation categorization by mental model) in the rule text, per this project's convention of naming the exact prior rule a new one sharpens rather than duplicates.
- [x] 6.2 Update the file's intro line ("Covers rules IA01-IA09") to include `IA10`, and add an applicability note mirroring the existing `IA01`/`IA09` pattern: `IA10` requires evidence that a goal genuinely spans multiple modules that could be jointly orchestrated, reporting `NOT ASSESSABLE` when that context is missing.
- [x] 6.3 Add a citation to Norman's expert-centric-taxonomy example (a field guide requiring the user to already know a bird is a "grebe"; medical lab results organized by which lab ran the test rather than by clinical symptom) to `IA01`'s and `IA04`'s Sources columns as reinforcing evidence for the existing mental-model-vs-internal-structure rules — no behavior change to either rule.
- [x] 6.4 Update `product/references/core.md`'s "How the product skill should apply these" section (step 3, where `information-architecture.md` is introduced) to mention `IA10` alongside the existing `IA01`-`IA09` set.

## 7. Product: just-in-time instructional delivery in `core.md`

- [x] 7.1 Add `P11` (instructional/coaching content for a feature is delivered in-context at the moment a user attempts or needs that feature, not front-loaded as a mandatory upfront tour/manual) to `src/skills/product/references/core.md`'s Purpose & task table, matching the "A mandatory multi-screen feature tour blocks first use", "A brief, skippable contextual tip appears at the moment of first use", and "Reference documentation exists separately from onboarding" scenarios in `specs/ux-crux/product/spec.md`; explicitly distinguish it from `P07` (first-use/repeat-use/expert-use differentiation, which does not specify delivery timing) in the rule text.
- [x] 7.2 Add a citation to Norman's just-in-time contextual-learning framing and the Kodak EasyShare contextual-demonstration example to `C04`'s Sources column, as reinforcing context distinct from but adjacent to `P11` — no behavior change to `C04`.
- [x] 7.3 Update `core.md`'s "How the product skill should apply these" numbered list to add a step covering `P11`, alongside the existing onboarding-adjacent steps (`P05`-`P08`).

## 8. Documentation and evidence trail

- [x] 8.1 Add Donald A. Norman, *Living with Complexity* (MIT Press, 2010), to `README.md`'s "Evidence base" list of comparison-pass sources.
- [x] 8.2 Recount every rule-table row across `src/skills/**/references/*.md` matching the `| <ID> | ... |` pattern (excluding folk-rule-guard tables and other non-rule descriptive tables), and update the total rule count stated in `README.md`'s opening paragraph to the new total (current stated total 323 + 7 new rows).
- [x] 8.3 Add a `CHANGELOG.md` entry (new version heading, one above the current `0.1.20` entry) in the same style as the `0.1.6`-`0.1.20` entries: what was added (by domain and rule ID: `AF05`, `S13`, `N23R`, `E09`, `PC09`, `IA10`, `P11`), what was left out and why (Frontstage/Backstage service architecture, Expressive Service Blueprinting, systems-level service root-cause redesign, physical queue-topology engineering, distribution-channel/middleman bias, organizational over-measurement critique — all process/service-methodology content, not a static-evidence UI/feature review concern), and what was confirmed already-covered (the Four Kinds of Affordances vocabulary and `AF02`'s user-workaround signal, `F21`/`PB01`/`PB02` defaults, `E01`'s error-prevention-over-messages, `IA01`/`IA04`'s mental-model-vs-internal-structure check, `PC06`'s interruption resilience, `F10`/`F11`'s password-friction reduction) plus the seven citation-only enrichments.
- [x] 8.4 Confirm no file under `openspec/changes/add-living-with-complexity-rules/` or `src/skills/**` references the private `local/` research folder by path or filename, per this repo's `CLAUDE.md` — grep for `local/` across both trees and confirm zero matches.

## 9. Build, version, and validate

- [x] 9.1 Bump `package.json`'s version from `0.1.20` to `0.1.22` per this project's one-SemVer-for-the-whole-plugin convention.
- [x] 9.2 Run `npm run build && npm run sync-version && npm run validate` and confirm success — this is the standing release gate for any change to `src/skills/**`.
- [x] 9.3 Confirm the rule-coverage checklist still accounts for every rule ID exactly once with no duplication across domains (per `openspec/config.yaml`'s specs rule), with particular attention to `AF05`/`S13`/`N23R`/`E09` staying in `usability`, `PC09` staying in `psychology`, and `IA10`/`P11` staying in `product`. Note: `E07`/`E08` were rejected as collision candidates with pre-existing `trust`'s `E07`/`E08`, and the new rule was assigned `E09`, the first free ID across the whole catalog's E-series (the pre-existing `O11` duplicate between `trust/core.md` and `trust/mobile.md` is unrelated to this change and left untouched).
