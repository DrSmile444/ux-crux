---
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill]
---

In our app, tapping the Search icon reveals a text input, but the user has to tap the input a second time before the keyboard shows up. Is that a UX problem, and what should we do about it?
