---
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill]
---

After the user taps "Search", the field appears but doesn't get focus and the keyboard doesn't pop up, so they have to tap it again. That's the whole issue — nothing about contrast, screen readers, or touch target size.
