---
type: llm
weight: 1
---

Pass if the response:
1. Follows the shared report contract shape: blockers/majors surfaced first, category health mentioned per lens actually applied, and a top-3 highest-impact list — not a single opaque score with no structure.
2. Covers findings from at least three different lenses, correctly attributed:
   - Trust: the all-at-once permission requests (out-of-context timing), and the irreversible charge with no confirmation/undo (should be `blocker`-level given real financial/irreversibility risk).
   - Accessibility: the 30x30dp touch target below the Android 48x48dp minimum (`blocker` or `major`).
   - Product or Usability: the "Place order" vs "Save for later" buttons having equal visual weight when one is clearly primary (hierarchy finding).
   - Psychology: the countdown/scarcity banner that resets every reload should be flagged as a fabricated-urgency ethical guardrail violation (deceptive pattern), not praised as good urgency design.
3. States which lenses were applied.
4. Does not silently omit the resetting-countdown scarcity issue or treat it as acceptable urgency messaging — a response that misses this or praises it should be marked down significantly.

Fail if the response only addresses one lens, presents a single unexplained score with no structure, or fails to flag the fabricated urgency / no-confirmation-charge issues as significant findings.
